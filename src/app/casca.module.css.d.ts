/**
 * Tipos das classes de `casca.module.css`. Escrito à mão pelo mesmo motivo dos
 * outros módulos de estilo: a declaração injetada é assinatura de índice, e nome de
 * classe errado compilaria.
 */
declare const estilo: {
  readonly casca: string;
  readonly colunaLateral: string;
  readonly conteudo: string;
  readonly grupo: string;
  readonly grupoTitulo: string;
  readonly lateral: string;
  readonly marca: string;
  readonly navegacao: string;
  readonly nomeDoSistema: string;
  readonly porta: string;
  readonly portaAtual: string;
  readonly portas: string;
  readonly rodape: string;
  readonly sigla: string;
};

export default estilo;
