/**
 * Tipos das classes de `publicar-em.module.css`, escritos à mão pelo mesmo motivo dos
 * outros módulos de estilo: a declaração injetada é assinatura de índice, e nome de
 * classe errado compilaria.
 */
declare const estilo: {
  readonly botaoSecundario: string;
  readonly link: string;
  readonly publicar: string;
  readonly publicarAcoes: string;
  readonly publicarLoja: string;
  readonly publicarNome: string;
  readonly publicarNota: string;
  readonly publicarTextos: string;
};
export default estilo;
